databaseChangeLog:
  - changeSet:
      id: assign-users-roles
      author: tsuki99
      changes:
        - insert:
            tableName: users_roles
            columns:
              - column:
                  name: user_id
                  valueComputed: "(SELECT id FROM users WHERE email='adminIvan@i.ua')"

              - column:
                  name: role_id
                  valueComputed: "(SELECT id FROM roles WHERE name='ADMIN')"

        - insert:
            tableName: users_roles
            columns:
              - column:
                  name: user_id
                  valueComputed: "(SELECT id FROM users WHERE email='userBob@i.ua')"

              - column:
                  name: role_id
                  valueComputed: "(SELECT id FROM roles WHERE name='USER')"

        - insert:
            tableName: users_roles
            columns:
              - column:
                  name: user_id
                  valueComputed: "(SELECT id FROM users WHERE email='alice@i.ua')"

              - column:
                  name: role_id
                  valueComputed: "(SELECT id FROM roles WHERE name='USER')"